# Rendering Layer

This directory is reserved for the production pet renderer.

Planned responsibilities:

- PixiJS bootstrap
- texture and atlas loading
- animation graph playback
- micro-motion controllers
- scene composition
- drag and hit-area controllers

The render loop should remain independent from React rendering.
